# ----------------------------------------------------
# DATABASE CONFIGURATION (MongoDB Atlas)
# ----------------------------------------------------
# CRITICAL: Replace <db_4289TGVIG> with your actual MongoDB password.
# The 'TGVIG_DB' at the end is the name of the database that Mongoose will use.
MONGODB_URI="mongodb+srv://TheGrooveVig_db_user:db_4289TGVIG@loyaltycluster.pe06khq.mongodb.net/TGVIG_DB?retryWrites=true&w=majority"

# ----------------------------------------------------
# APPLICATION SECRETS
# ----------------------------------------------------
# Used for signing JSON Web Tokens (JWT) for secure user sessions.
# This should be a long, random, hard-to-guess string.
JWT_SECRET="THEGROOVEVIG-VALUEDIMPORTANTGROOVIST"

# ----------------------------------------------------
# PORT (Optional, used by Render/Heroku if provided)
# ----------------------------------------------------
PORT=3000